import { makeStyles } from '@material-ui/core';
const useStyles = makeStyles((theme) => ({
  itemName: {
   
  },
  createIcon: {
    // position: "absolute",
    visibility: "hidden"
  }
}));

export default useStyles;
